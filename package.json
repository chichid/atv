{
  "name": "atv",
  "private": "true",
  "version": "1.0.0",
  "homepage": "https://github.com/chichid/atv-test#readme",
  "scripts": {
    "postinstall": "lerna exec npm install",
    "start": "lerna run --parallel start --scope=\"{$START_SERVICES}\"",
    "watch": "lerna run --parallel watch --scope=\"{$START_SERVICES}\""
  },
  "dependencies": {
    "lerna": "^3.22.1"
  }
}
