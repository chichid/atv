<?xml version="1.0" encoding="UTF-8"?>
<atv>
  <head>
    <script src="/tv-service/templates/utils.js" />
  </head>
  <body>
    <listScrollerSplit id="listScoller">
      <header>
        <simpleHeader accessibilityLabel="Movie list">
          <title>Canaux</title>
        </simpleHeader>
      </header>
      <menu id="mainMenu">
        <sections>
          <menuSection id="channelGroupsMenuSection">
            <items>
              {{#fetch '/tv-service/channels'}}
              <oneLineMenuItem id="{{GroupName}}">
                <label>{{GroupName}}</label>
                <preview>
                  <scrollerPreview id="scrollerPreview-{{GroupName}}">
                    <items>
                      <grid id="channelsGrid-{{GroupName}}" columnCount="4">
                        <items>
                          {{#each channels}}
                          <sixteenByNinePoster id="{{Channel}}" alwaysShowTitles="true" 
                            onSelect="playVideo('{{StreamUrl}}')"
                            onHoldSelect="atv.loadURL('/tv-service/templates/epg.xml?Channel={{Channel}}')">
                            <title>{{#if timeshiftURL}}*{{/if}} {{Channel}}</title>
                            <image src720="{{LogoUrl}}" src1080="{{LogoUrl}}" />
                            <defaultImage>resource://16x9.png</defaultImage>
                          </sixteenByNinePoster>
                          {{/each}}
                        </items>
                      </grid>
                    </items>
                  </scrollerPreview>
                </preview>
              </oneLineMenuItem>
              {{/fetch}}
            </items>
          </menuSection>
        </sections>
      </menu>
    </listScrollerSplit>
  </body>
</atv>
