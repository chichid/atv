<?xml version="1.0" encoding="UTF-8"?>
<atv>
  <head>
    <script src="{{config.BaseUrl}}/assets/js/utils.js" />
  </head>
  <body>
    <scroller id="mainScroller">
      <items>
        {{#each groups}}
        <collectionDivider alignment="left" accessibilityLabel="{{groupName}}">
          <title>{{groupName}}</title>
        </collectionDivider>

        <grid id="channelsGrid-{{groupName}}">
          <items>
            {{#each channels }}
            <squarePoster id="{{name}}" alwaysShowTitles="true" onSelect="playVideo('{{url}}')">
              <title>{{name}}</title>
              <image src720="{{logo}}" src1080="{{logo}}" />
              <defaultImage>resource://Square.png</defaultImage>
            </squarePoster>
            {{/each}}
          </items>
        </grid>
        {{/each}}
      </items>
    </scroller>
  </body>
</atv>
